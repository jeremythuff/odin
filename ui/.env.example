# Client (web UI) environment variables
NODE_ENV=development
HOST=0.0.0.0
PORT=3000
CLIENT_PORT=3000
# Optional: fully-qualified base URL for the API (overrides other API_* values)
API_BASE_URL=
# Optional: override protocol/host/port parts when deriving the API URL dynamically
API_PROTOCOL=
API_HOST=
API_PORT=8111
